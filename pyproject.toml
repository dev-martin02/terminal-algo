[project]
name = "terminal-algo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.39.0",
    "typer>=0.26.5",
]

[project.scripts]
terminal-algo = "main:app"
